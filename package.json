{
  "name": "cf-worker-builder",
  "version": "1.0.0",
  "description": "对话式 Cloudflare Worker 在线构建器：接入 OpenAI 兼容大模型（DeepSeek 等），对话生成 Worker 代码并自动部署到 Cloudflare Workers",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "cloudflare-workers",
    "cloudflare",
    "deepseek",
    "openai",
    "ai-agent",
    "code-generator",
    "worker-builder"
  ],
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
